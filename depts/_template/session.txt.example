# Telegram gram.js StringSession
# 取得方式：
#   1. 填好 .env 的 TG_API_ID 和 TG_API_HASH
#   2. cd depts/<dept_name>
#   3. node ../../scripts/login-dept.js
#   4. 依提示輸入手機號 / 驗證碼 / 兩步驗證密碼
#   5. 腳本會把 StringSession 寫入 session.txt
#
# ⚠️ session.txt 是登入憑證，洩漏 = TG 帳號被接管
# ⚠️ 不要上傳到 git（已在 .gitignore）
# ⚠️ 建議加密備份到另一處（如 1Password / age 加密檔）
