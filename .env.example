# Docker Compose 的 .env (非敏感, 僅部署層參數)
#
# 敏感資料不在這裡:
#   - TG_API_ID / TG_API_HASH 在 depts/<name>/.env 和 global/<kind>/.env (Web setup 寫入)
#   - Google SA 在 secrets/google-service-account.json (Web setup 上傳)
#   - Web 密碼在 data/system.json (Web setup 寫入)

# Web 對外埠
WEB_PORT=5003
